import {NavLink,Outlet} from 'react-router-dom';import {BriefcaseBusiness,LayoutDashboard,Users,UserRound,LogOut,Moon,Sun} from 'lucide-react';import {useAuth} from '../context/AuthContext';import {useEffect,useState} from 'react';export default function Layout(){const {user,logout}=useAuth();const [dark,setDark]=useState(()=>localStorage.theme==='dark');useEffect(()=>{document.documentElement.dataset.theme=dark?'dark':'light';localStorage.theme=dark?'dark':'light'},[dark]);return <div className="app-shell"><aside><div className="brand"><BriefcaseBusiness/><span>SmartHire</span></div><nav><NavLink to="/"><LayoutDashboard/>Dashboard</NavLink><NavLink to="/jobs"><BriefcaseBusiness/>Jobs</NavLink><NavLink to="/applicants"><Users/>Applicants</NavLink><NavLink to="/profile"><UserRound/>Profile</NavLink></nav><div className="aside-bottom"><button onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>}{dark?'Light mode':'Dark mode'}</button><button onClick={logout}><LogOut/>Sign out</button></div></aside><main><header className="topbar"><div><b>{user?.company||'Recruitment workspace'}</b><small>Welcome, {user?.name}</small></div></header><div className="content"><Outlet/></div></main></div>}
