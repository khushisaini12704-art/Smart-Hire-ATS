import {useState,type FormEvent} from 'react';import {Link,useNavigate} from 'react-router-dom';import {BriefcaseBusiness} from 'lucide-react';import {useAuth} from '../context/AuthContext';import {errorMessage} from '../api/client';import {toast} from 'react-toastify';
export default function Auth({register=false}:{register?:boolean}){const auth=useAuth(),nav=useNavigate(),[busy,setBusy]=useState(false),[form,setForm]=useState({name:'',email:'',password:'',company:''});const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);try{if(register)await auth.register(form);else await auth.login(form.email,form.password);toast.success(register?'Account created':'Welcome back');nav('/')}catch(e){toast.error(errorMessage(e))}finally{setBusy(false)}};return <div className="auth-page"><div className="auth-hero"><div className="brand"><BriefcaseBusiness/>SmartHire</div><h1>Hire the right people, faster.</h1><p>A focused applicant tracking system for modern recruiting teams.</p></div><form className="auth-card" onSubmit={submit}><h2>{register?'Create recruiter account':'Recruiter sign in'}</h2>{register&&<><label>Full name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Company<input required value={form.company} onChange={e=>setForm({...form,company:e.target.value})}/></label></>}<label>Email<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input type="password" required minLength={8} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{register&&<small>Use 8+ characters with uppercase, lowercase, and a number.</small>}<button className="primary" disabled={busy}>{busy?'Please wait…':register?'Create account':'Sign in'}</button><p>{register?'Already registered?':'New to SmartHire?'} <Link to={register?'/login':'/register'}>{register?'Sign in':'Create account'}</Link></p></form></div>}
