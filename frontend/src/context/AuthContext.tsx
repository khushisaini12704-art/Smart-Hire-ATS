/* eslint-disable react-refresh/only-export-components */
import {createContext,useContext,useMemo,useState,type ReactNode} from 'react';import api from '../api/client';import type {User} from '../types';
interface AuthValue{user:User|null;login:(email:string,password:string)=>Promise<void>;register:(data:{name:string;email:string;password:string;company:string})=>Promise<void>;logout:()=>void;setUser:(u:User)=>void}
const C=createContext<AuthValue|null>(null);export function AuthProvider({children}:{children:ReactNode}){const [user,setUser]=useState<User|null>(()=>{try{return JSON.parse(localStorage.getItem('user')||'null')}catch{return null}});const save=(data:{token:string;user:User})=>{localStorage.setItem('token',data.token);localStorage.setItem('user',JSON.stringify(data.user));setUser(data.user)};const value=useMemo(()=>({user,login:async(email:string,password:string)=>save((await api.post('/auth/login',{email,password})).data),register:async(data:{name:string;email:string;password:string;company:string})=>save((await api.post('/auth/register',data)).data),logout:()=>{localStorage.clear();setUser(null)},setUser:(u:User)=>{setUser(u);localStorage.setItem('user',JSON.stringify(u))}}),[user]);return <C.Provider value={value}>{children}</C.Provider>}export const useAuth=()=>{const v=useContext(C);if(!v)throw new Error('AuthProvider missing');return v};
